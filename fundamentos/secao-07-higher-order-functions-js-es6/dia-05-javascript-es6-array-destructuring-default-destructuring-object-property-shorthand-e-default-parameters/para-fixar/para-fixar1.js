const primeNumbers = [17, 23, 37]

const [firstPrime, secondPrime, thridPrime] = primeNumbers;

const sum = (a, b) => {
  console.log(a + b);
}

sum(firstPrime, thridPrime); // 54

// Produza o mesmo resultado acima, porém utilizando array destructuring